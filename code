pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'git checkout'
                sh 'https://github.com/muzammilpasha4/DevOpsClassCodes'
            }
        }
        stage('compilinng'){
            steps{
                sh 'mvn compile'
            }
        }
    }
}
